/**
 * Page 1 — CRUD đơn hàng (ORDER).
 * Backend tương ứng: CineB-/backend/page1/ (mount /api/orders trong server.js).
 *
 * Cách mở rộng: src/page2 + backend/page2 tương tự; không sửa ./page1 nếu không làm Order.
 */
export { default as Page1OrderView } from "./OrderPage.jsx";
